export TX_FLAGS='--node http://mayanode:26657 --keyring-backend file --keyring-dir /cli/keys --chain-id mayachain'

cat <<EOF
>>> MAYANode Mocknet CLI <<<
1. The passphrase for all keys is "password"
2. The key named "dog" is an admin MIMIR key for the mocknet cluster
EOF
